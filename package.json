{
  "name": "photography-website",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint . --ext .ts,.vue --config .eslintrc",
    "lint:fix": "eslint . --ext .ts,.vue --config .eslintrc --fix",
    "lint:config": "eslint --config .eslintrc --print-config .eslintrc",
    "login": "vercel login",
    "vbuild": "vercel pull && vercel build",
    "deploy": "vercel deploy --prebuilt --prod"
  },
  "devDependencies": {
    "@nuxt/image": "1.0.0-rc.1",
    "@nuxtjs/eslint-config-typescript": "12.0.0",
    "@nuxtjs/tailwindcss": "6.8.0",
    "@vueuse/core": "10.2.1",
    "@vueuse/nuxt": "10.2.1",
    "eslint": "8.44.0",
    "eslint-plugin-nuxt": "4.0.0",
    "eslint-plugin-tailwindcss": "3.13.0",
    "nuxt": "3.6.1",
    "slugify": "1.6.6",
    "vercel": "31.2.0"
  }
}
